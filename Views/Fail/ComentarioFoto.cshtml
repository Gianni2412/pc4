@if (Model.Count == 0) {
    <div class="alert alert-warning text-center">Aún no hay fotos</div>
}else{
    <ol>
    @foreach (var c in Model) {
        <li class="d-flex">
            <div>@c.Foto</div>
        </li>
    }
    </ol>
    <form method="POST">
    <div class="form-group">
        <label asp-for="ComentarioId">Comentario:</label>
        <textarea asp-for="ComentarioId" class="form-control"></textarea>
    </div>
    <button class="btn btn-primary">Subir Comentario</button>
    
</form>
}
